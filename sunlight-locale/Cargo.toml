[package]
name = "sunlight-locale"
version = "0.2.2141-alpha.1"































































































































































edition = "2021"

[features]
default = []
std = []

[dependencies]
# Keep minimal; no_std + alloc friendly for kernel/userspace use
