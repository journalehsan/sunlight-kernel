[package]
name = "sunlight-tui"
edition = "2021"

[dependencies]
# NO external dependencies — pure no_std, no heap

[build-dependencies]
fontdue = "0.7"

[features]
default = []
# Compile-time mode selection (can be overridden by runtime flag too)
shutup-mode = []   # enables silent mode as default
