[package]
name = "sunlight-wallpaper"
version = "0.2.4163-alpha.1"



























































































































































































































































































edition = "2021"
description = "Minimal wallpaper config and scanning helpers for SunlightOS"
license = "MIT"

[dependencies]
sunlight-libc = { path = "../sunlight-libc" }
# SIMG v2 header validation (no_std + alloc).
sun-img = { path = "../sun-img", default-features = false }
